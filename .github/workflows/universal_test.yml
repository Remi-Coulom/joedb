name: Universal test

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{matrix.os}}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-2025-vs2026, windows-2025, windows-2022, ubuntu-24.04, ubuntu-22.04, macos-26, macos-15]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Configure CMake
        working-directory: ${{github.workspace}}/compcmake
        shell: bash
        run: cmake -B build -DCMAKE_BUILD_TYPE=Debug

      - name: Test
        uses: threeal/ctest-action@v1.1.0
        with:
          test-dir: ${{github.workspace}}/compcmake/build
          verbose: true
          build-config: Debug
