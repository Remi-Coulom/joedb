#include "joedb/Signal.h"

#include "gtest/gtest.h"

namespace joedb
{
 /////////////////////////////////////////////////////////////////////////////
 TEST(Signal, basic)
 /////////////////////////////////////////////////////////////////////////////
 {
  Signal::start();
  Signal::stop();
  Signal::set_signal(1234);
  EXPECT_EQ(Signal::get_signal(), 1234);
 }
}
