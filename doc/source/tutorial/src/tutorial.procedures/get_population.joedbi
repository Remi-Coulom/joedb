create_table input
add_field input city_name string

# "city" is the name of a table in the main schema
# The line below allows refering to the main schema from the procedure schema
create_table city

create_table output
add_field output city references city
add_field output population int64
